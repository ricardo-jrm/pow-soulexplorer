import React from 'react';
import type { NextPage } from 'next';
import { ViewToken } from '../../containers/ViewToken';

export const TokenPage: NextPage = () => <ViewToken />;

export default TokenPage;
